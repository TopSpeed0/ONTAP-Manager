# =============================================================================
# Vault-encrypted credentials for ONTAP cluster access
# =============================================================================
# Encrypt this file with:
#   ansible-vault encrypt vault_credentials.yml
#
# Then run playbooks with:
#   ansible-playbook provision_s3_bucket.yml --ask-vault-pass -e "bucket_name=xxx"
#   ansible-playbook provision_s3_bucket.yml --vault-password-file ~/.vault_pass -e "bucket_name=xxx"
# =============================================================================

ontap_password: "CHANGEME_REPLACE_WITH_ACTUAL_PASSWORD"
